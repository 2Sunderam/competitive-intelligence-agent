# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv

# Secrets / local env
.env

# Internal design notes — not part of the deliverable
docs/

# Run outputs are committed on purpose — the assignment asks for generated
# outputs in the repo, and the evidence files are the proof that every claim
# is grounded in a real quote from a real URL.
